import React, { useEffect, useState } from 'react';
import managerData from '../constants/manager_details.json';

const H2H = ({ currentGW, gameweekFinished, fixtures, selectedManager }) => {

    return (
        <div>This is a placeholder</div>
    )
};

export default H2H;